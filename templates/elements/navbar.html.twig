<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.min.js" integrity="sha384-QJHtvGhmr9XOIpI6YVutG+2QOK9T+ZnN4kzFN1RtK3zEFEIsxhlmWl5/YESvpZ13" crossorigin="anonymous"></script>
<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">Navbar</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="true" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="navbar-collapse collapse show" id="navbarColor01" style="">
            <ul class="navbar-nav me-auto">
                <li class="nav-item active">
                    <img class="img-fluid" src="{{ asset('assets/images/titleShip.png') }}">
                    <a class="nav-link" href="{{ path('home') }}">Home
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>

                {% if is_granted("ROLE_ADMIN") %}

                    <li class="nav-item">
                        <a class="nav-link" href="#">Navires </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Pays et Ports </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_register') }}">Compte </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}">Se déconnecter </a>
                    </li>

                {% elseif is_granted("ROLE_OPER") or is_granted("ROLE_USER")%}

                    <li class="nav-item">
                        <a class="nav-link" href="#">Navires </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Pays et Ports </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}">Se déconnecter </a>
                    </li>

                {% else %}

                    <li class="nav-item">
                        <a class="nav-link" href="#">Pays </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login') }}">Se Connecter </a>
                    </li>

                {% endif %}
            </ul>
        </div>
        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            <h2> {{ app.user.nom }} {{app.user.prenom }}</h2>
        {% else %}
            <h2> anonymous </h2>
        {% endif %}
        <div>
            {{ render(controller('App\\Controller\\SearchController::searchBar')) }}
        </div>
    </div>
</nav>

